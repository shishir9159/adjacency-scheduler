apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adjacency-grpc
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/backend-protocol: "GRPC"
  labels:
    app: adjacency
spec:
  ingressClassName: nginx
  rules:
    - host: adjacency-grpc.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: adjacency
                port:
                  number: 50051