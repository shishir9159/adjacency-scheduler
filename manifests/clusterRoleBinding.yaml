apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: adjacency
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: adjacency
subjects:
  - kind: ServiceAccount
    name: adjacency
    namespace: default